# Sin pins de version a proposito.
#
# El requirements anterior fijaba el stack de julio de 2026 (torch 2.2.1,
# numpy 1.26.4, albumentations 1.3.1, smp 0.3.3). Colab paso a Python 3.13 y
# ninguno de esos instala ya: pip aborta la instalacion entera y el entorno se
# queda sin segmentation-models-pytorch.
#
# Fijar el stack viejo tampoco arreglaria las comparaciones. Albumentations 2.x
# descarta en silencio argumentos que la 1.x aceptaba, asi que un run de hoy no
# es comparable con uno de julio de todos modos. Los notebooks resuelven eso
# entrenando su propio baseline en la misma sesion, de forma que el entorno es
# comun a los dos lados de la comparacion y se cancela. Cada run deja constancia
# de sus versiones en debug_fingerprint.json, y las celdas de resumen avisan si
# los brazos que se comparan no las comparten.
#
# torch y torchvision van sin pin para que pip acepte los que Colab ya trae
# instalados en vez de reinstalarlos.
numpy
pandas
matplotlib
opencv-python-headless
Pillow
albumentations
timm
torch
torchvision
segmentation-models-pytorch
scipy
scikit-image
tqdm
ml_collections
